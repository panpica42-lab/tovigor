<!-- markdownlint-disable -->
# Tovigor 开发日志（精简版）

> 记录项目开发过程中的关键节点和功能实现

---

## 2026-02-27

### 首页控制面板

- **移除原生导航栏**：`pages.json` 添加 `navigationStyle: custom`，首页改为全自定义布局
- **使用指南按钮调整**：从左侧绝对定位改为右侧 flex 布局，与标题对齐
- **新增控制小面板**：
  - 6个图标：亮度、WiFi、音量、蓝牙、设置、开关
  - 圆润长方形外观（小圆角 + 阴影凹凸感）
  - 点击切换状态：深灰色 ↔ 绿色

### 智能评估模块重构

- **目录结构调整**：三大评估模块各自独立为文件夹
  ```
  smartAssess/
  ├── first-skip.vue                  # 入口页（保持根目录）
  ├── body-function-assess/           # 身体机能评估
  ├── body-strength-assess/           # 部位力量评估（原 strength-assess）
  │   ├── body-strength-assess-new.vue
  │   └── components/
  │       └── body-part-card.vue
  └── body-posture-assess/            # 体态姿势评估（新建）
  ```
- **命名规范统一**：所有评估模块统一为 `body-xxx-assess` 三段式命名
- **力量评估入口页**：
  - 新建 `body-strength-assess-new.vue`，旧版保留作为代码资源库
  - 人体模型 + 6个部位卡片布局
  - 抽象 `body-part-card.vue` 组件，显示部位名称、重量、三个子项（热身/训练/挑战）
  - 使用公共返回按钮组件 `CommonBackButton`
- **路由更新**：`pages.json` 和 `first-skip.vue` 跳转路径同步更新

---

## 2026-02-10

### 正式训练页 - 力量控制面板优化

- **组件抽象**：将训练数据看板（组数、热量、心率）抽象为 `DataPanel` 通用组件
- **自定义滑块**：实现纯代码力量滑块，支持触摸拖动和按钮调节
- **开关逻辑**：进入页面默认关闭，关闭状态滑块变灰且不可操作
- **性能优化**：解决滑块拖动"不跟手"问题，详见 [难点解决](./开发日志(难点解决).md)

---

## 2026-02-28

### 调试模式优化

- **串口连接开关**：`App.vue` 新增 `DEBUG_MODE` 常量，设为 `true` 时跳过串口初始化，避免热更新频繁重连

### 踩坑记录：flex 容器中图片不显示

**场景**：力量评估页的人体模型图片设置 `height: 100%` 后不显示

**原因**：
- 父元素 `.body-model-area` 使用 `flex: 1`，高度是弹性计算的，没有明确像素值
- 子元素 `<image>` 设置 `height: 100%` 时，找不到确定的参照值，计算结果为 0
- `width: auto` 依赖 height 计算，height 为 0 则 width 也为 0，图片消失

**解决方案**：改用固定尺寸 `width: 500rpx; height: 900rpx;`，不依赖父元素高度

### 规则总结：flex 容器中的百分比高度

**不是绝对不能用，而是有条件：**

| 父元素高度 | 子元素 `height: 100%` | 结果 |
|-----------|----------------------|------|
| `height: 500px` / `100vh` / 固定rpx  |  有参照值 | 生效 |
| `height: auto`（内容撑开）            |  无参照值 | 为 0 |
| `flex: 1`（弹性计算）                 |  无参照值 | 为 0 |
| `flex: 1` + `height: 0`              |  强制基准 | 生效（hack） |

**问题链条示例**：
```
.page (height: 100vh)              ✓ 明确
  └── .content (flex: 1)           ✗ 弹性，无明确值
        └── .body-model-area (flex: 1)   ✗ 弹性，无明确值
              └── .body-image (height: 100%)  → 找不到参照！
```

**安全做法**：
1. **图片/固定内容**：直接用 `rpx` 固定尺寸
2. **需要撑满的容器**：用 `position: absolute; top:0; bottom:0;`
3. **flex 子项要用百分比**：给父元素加 `height: 0`（强制基准）

**排查口诀**：flex 容器里的东西不显示 → 先检查百分比高度链条

---

## 2026-03-01 ~ 03-04

### AI 教练系统

- **全局教练状态管理**：`utils/coachManager.js`
  - 支持切换教练（Vince / Vera）
  - 本地存储持久化（`uni.setStorageSync`）
  - 提供 `getSelectedCoach()`、`setSelectedCoach()` 等 API
- **教练详情弹窗**：`components/modals/coach-detail-modal.vue`
  - 支持查看模式（只看详情）和切换模式（选择教练）
  - 教练卡片展示：头像、姓名、简介、标签
- **气泡对话框组件**：`components/ui-box/bubble-dialog-box.vue`
  - 上方小气泡（角色名 + 头像）+ 下方大气泡（对话内容）
  - 支持点击交互（`@coach-click` 事件唤起弹窗）
  - 在力量评估页集成，点击气泡可切换教练

### 通信架构 v3（读发分离）

详见专项文档 `docs/开发日志/2026-02-12-通信架构重构.md`

**核心改动**：
- 摒弃 App 启动即轮询的 v2 架构
- 新增 `startWorking()` / `stopWorking()` 按需启停
- 解决控制台 `Failed to execute the callback function` 报错（页面切换后回调 ID 失效）
- JS 服务层（`serialService.js`）充当通信中介者和数据解析 Kernel，App 启动时串口即连接，Vue 页面只检查连接状态不尝试连接，页面按钮按需控制通讯启停
- 正式训练页完成通信集成

### 自由训练模块新增页面

- **训练介绍页**：`pages/freeTraining/free-training-intro.vue`
- **动作库页面**：`pages/freeTraining/action-library.vue`

### UI 组件扩展

- **筛选胶囊**：`components/ui-box/filter-pill.vue`
- **力量滑块**：`components/ui-box/power-slider.vue`（从训练页抽离）
- **步骤进度条**：`components/ui-box/step-bar.vue`

---

## 2026-03-05

### 力量评估模块文件重命名

为提高可读性，对 `body-strength-assess/` 目录下的文件进行重命名：

| 旧文件名 | 新文件名 | 说明 |
|---------|---------|------|
| `body-strength-assess.vue` | `strength-display-old.vue` | 旧版力量展示（待删除，代码资源库） |
| `body-strength-assess-new.vue` | `strength-overview.vue` | 力量概览入口页（人体模型+部位卡片） |
| `skip1.vue` | `strength-assess.vue` | 力量评估页（AI教练+配重滑块） |

**同步更新**：
- `pages.json` 路由配置
- `first-skip.vue` 跳转路径
- `strength-overview.vue` 内部跳转

---
